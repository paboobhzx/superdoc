# 2026-05-26

## Goals
- Fix `pdf_to_docx` regression so explicit text fallback never switches to image-page DOCX.
- Add scanned table-aware analysis with XLSX recommendation for table-like PDFs.
- Update EN/PT parameter and recommendation copy for clarity.
- Add AWS cost-allocation feature tags for this rollout and keep forward-fix strategy.

## Actions Taken
- Enforced strict fallback contract in `handlers/pdf_to_docx.py`:
  - `fallback_strategy=text` now disables hybrid/image-style override paths.
  - QA fallback keeps text reconstruction only for text fallback.
- Extended classifier and analysis outputs:
  - Added per-page `likely_table_image` and `table_confidence`.
  - Added summary `table_like_pages` and `has_table_like_scanned_content`.
  - Added analyzer recommendation rule: table-like scanned content -> `recommendation: "xlsx"`.
- Updated UI behavior and copy:
  - New fallback label wording in EN/PT.
  - Added XLSX recommendation rendering path in analysis strip.
  - Updated scanned warning and rationale strings in EN/PT.
- Added tests:
  - Regression tests for strict text fallback and high-fidelity override blocking.
  - Classifier tests for table-like scanned classification fields.
  - Frontend test for XLSX recommendation display/checkbox behavior.
- Added infra tag dimension:
  - New Terraform variable `cost_allocation_feature`.
  - Added `CostAllocationFeature` to `local.common_tags`.

## Cost Notes
- Table-like detection adds lightweight page raster analysis during `pdf_analyze`, increasing Lambda CPU time modestly for larger PDFs.
- OCR usage policy is unchanged; cost impact is from analysis runtime and expected to be minor versus OCR conversion paths.
- `CostAllocationFeature` tag is included in common tags to make this rollout visible in cost-allocation tag reporting.
