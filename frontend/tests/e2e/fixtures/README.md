# E2E fixtures

Run `python3 scripts/generate_test_fixtures.py` from the repo root to regenerate the sample files in `samples/`.

The generator is deterministic and only uses the Python standard library, so it can run in a bare workspace.
